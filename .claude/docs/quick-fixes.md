- delete entities (only admin)
- dialogs in html and general ui improvements
- unit tests for the ui

- skills to improve LLM performance in converting JSONB to flat fields and vice-versa (specific to duckdb without locking)
- discuss transformations should be triggered by data changes (consider relationships across tables), or schedules
- cached layer (redis/cloudflare) with api / graphql (only silver + gold tables)
- agent memory to remember routines / solutions and context injection
- context pruning and compaction
- transformation plugins (skill creator, python executables with minimal dependencies - containerised dependency installation per skill)
- code refactoring in smaller files / organisation / readability without impacting LLM context reading